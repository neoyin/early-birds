理解java proxy机制
代理模式支持将某些操作从实际的对象中分离出来，通过它的代理类提供处理。这样便于修改和管理这些特定的操作。

先看下提供的几个类, Employee是员工, PaySalary.parSalary()方法接收一个Employee, 打印员工的实际发的工资.
PaySalaryImpl是一个默认实现类.

题目: 现在发工资的规则变了, 工资大于4500的员工, 只发70%, 小于等于4500的员工发全部, 
要求实现一个PaySalaryImpl的代理类, 加入这些规则,修改员工的income属性, 再调用默认实现.

测试类自己写