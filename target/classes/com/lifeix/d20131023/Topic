可变字节码数字读取

题目:
1. 实现IntDecoder的两个接口方法
2. splitBytes()根据上次的编码规则, 把输入流切成每个整数一个byte[],返回所有byte[]的list, parseInt()把byte[]转换成整数.

ps:
1. 实现方法里不要有输出信息, 提交时把调试的输出注释掉.
2. 上周的题目输出的都不对, 输出的是整数编码的byte数组, 不是它们的字符串形式的byte数组,参照一下李江的输出改一下(虽然他的顺序也不对), 下次还要用的.